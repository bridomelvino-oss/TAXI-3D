// MapZone est remplacé par MapLeaflet (Leaflet.js + OpenStreetMap).
// Ce fichier est conservé vide pour éviter les erreurs d'import résiduel.
export default function MapZone(): null { return null; }
